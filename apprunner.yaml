version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm ci
      - npx prisma generate
      - npm run build
run:
  command: npm run start:migrate:prod
  network:
    port: 8001
  env:
    - name: NODE_ENV
      value: production
