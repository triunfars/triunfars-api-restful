version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm ci
      - npx prisma generate
      - npm run build
run:
  command: npm run start:prod
  network:
    port: 8001
  env:
    - name: NODE_ENV
      value: production
